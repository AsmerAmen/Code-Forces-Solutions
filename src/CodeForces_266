
#include <iostream>
using namespace std;

int main() {
	int count;
	char *arr;
	int min=0;
	cin>>count;
	arr = new char[count];
	
	for(int i=0; i<count; i++)
		cin>>arr[i];

	for(int i=0; i<count; i++)
		if(arr[i] == arr[i+1])
			min++;

	cout<<min<<endl;

	return 0;
}
